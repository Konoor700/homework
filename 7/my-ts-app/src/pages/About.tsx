import React from 'react';
import { Typography, Box } from '@mui/material';

const About: React.FC = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h2">About Project</Typography>
    <Typography variant="body1">Homework #7: React, TypeScript, MUI, and Axios.</Typography>
  </Box>
);

export default About;
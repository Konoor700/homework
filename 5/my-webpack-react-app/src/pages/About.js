import React from 'react';
import { Typography, Box } from '@mui/material';

const About = () => (
  <Box>
    <Typography variant="h2">About Project</Typography>
    <Typography variant="body1">Homework #6: React Router, MUI, and Axios.</Typography>
  </Box>
);

export default About;
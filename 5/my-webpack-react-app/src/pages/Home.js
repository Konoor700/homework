import React from 'react';
import { Typography, Box } from '@mui/material';

const Home = () => (
  <Box>
    <Typography variant="h2">Welcome Home</Typography>
    <Typography variant="body1">This is the main page of the Rick and Morty app.</Typography>
  </Box>
);

export default Home;